[Flyzx]
  >>Considero que seria mas optimo un Kernel Estatico
  >>Ejem
    >>> clave = Kernel.modularHash(NumElementos, Dato);
    >>> clave = Kernel.cuadradoHash(NumElementos, Dato);

    >>Es mas practico. Yo lo veo bien : Lo dejo implementado de una vez.